# OAuth Client ID de Google Cloud Console (tipo "Web application").
# No es secreto (los Client ID de OAuth son públicos por diseño), pero se
# configura por entorno para no atarlo al código. Ver README.md para los
# pasos de configuración en Google Cloud Console.
VITE_GOOGLE_CLIENT_ID=
